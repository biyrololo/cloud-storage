# Cloud Storage webapp on Next.js

A simple cloud storage webapp on [Next.js](https://nextjs.org)

## Getting Started

Create database, set up environment variables, set environment variables for oauth and run the development server:

```bash
npm run dev
```

Open [http://localhost:3000](http://localhost:3000) with your browser to see the result.
